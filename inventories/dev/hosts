# Development inventory – everything runs on a single machine

[all]
# Replace the IP address below with your actual dev server's public IP
# Use the appropriate SSH user depending on your EC2 image:
# - ubuntu      → for Ubuntu AMIs (e.g., Ubuntu 22.04)
# - ec2-user    → for Amazon Linux
# For development, the entire monitoring stack runs on a single host
    # If needed, you can extend this for more dev nodes later
dev ansible_host=127.0.0.1 ansible_port=2222 ansible_user=ansible ansible_ssh_private_key_file=/home/ilona/ansible-test/test_key
